services:
  ai-server:
    environment:
      AI_SNAPSHOT_POLICY: INCIDENT_ONLY
  frontend-web:
    volumes:
      - type: bind
        source: ./artifacts/presentation-demo
        target: /app/public/demo
        read_only: true
